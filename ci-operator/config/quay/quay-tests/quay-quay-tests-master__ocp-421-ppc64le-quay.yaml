base_images:
  cli:
    name: "4.21"
    namespace: ocp
    tag: cli
  upi-installer:
    name: "4.21"
    namespace: ocp
    tag: upi-installer
build_root:
  image_stream_tag:
    name: release
    namespace: openshift
    tag: rhel-9-release-golang-1.25-openshift-4.21
prowgen:
  expose: true
  private: true
releases:
  latest:
    candidate:
      architecture: ppc64le
      product: ocp
      stream: nightly
      version: "4.21"
resources:
  '*':
    limits:
      memory: 4Gi
    requests:
      cpu: 100m
      memory: 200Mi
tests:
- as: quay-e2e-tests-libvirt-ppc64le
  capabilities:
  - intranet
  cron: '@yearly'
  reporter_config:
    channel: '#quay-qe'
    job_states_to_report:
    - success
    - failure
    - error
    report_template: '{{if eq .Status.State "success"}} :slack-green: Job *{{.Spec.Job}}*
      ended with *{{.Status.State}}*. <{{.Status.URL}}|View logs> {{else}} :failed:
      Job *{{.Spec.Job}}* ended with *{{.Status.State}}*. <{{.Status.URL}}|View logs>
      {{end}}'
  steps:
    cluster_profile: libvirt-ppc64le-s2s
    dependencies:
      OPENSHIFT_INSTALL_TARGET: release:ppc64le-latest
    env:
      ARCH: ppc64le
      BRANCH: "4.21"
      DOMAIN_MEMORY: "34816"
      ETCD_DISK_SPEED: slow
      USE_RAMFS: "true"
      CSO_CHANNEL: stable-3.17
      CSO_SOURCE: fbc-operator-catalog
      MULTISTAGE_PARAM_OVERRIDE_QUAY_INDEX_IMAGE: quay.io/redhat-user-workloads/quay-eng-tenant/stable-3-17-v4-21@sha256:bc002f7dff4f1c3973c26f34b4b7cf869cef3965d49469b1f06bd6b5416c13b4
      QBO_CHANNEL: stable-3.17
      QBO_SOURCE: fbc-operator-catalog
      QUAY_OPERATOR_CHANNEL: stable-3.17
      QUAY_OPERATOR_SOURCE: fbc-operator-catalog
      QUAY_VERSION: "3.17"
    test:
    - ref: quay-tests-enable-quay-catalogsource
    - ref: quay-tests-deploy-quay-aws-s3
    - ref: quay-tests-test-quay-e2e
    - ref: quay-tests-qbo-qe-test
    - ref: quay-tests-cso-qe-test
    workflow: openshift-upi-libvirt
zz_generated_metadata:
  branch: master
  org: quay
  repo: quay-tests
  variant: ocp-421-ppc64le-quay
